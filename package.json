{
  "name": "rigi-docs",
  "version": "1.0.0",
  "description": "The Rigidas Software applications documentation.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "npm run generate"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "nuxt": "^3.4.3"
  },
  "config": {
    "nuxt": {
      "host": "0.0.0.0",
      "port": "10000"
    }
  },
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
